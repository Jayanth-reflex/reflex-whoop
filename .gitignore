# Xcode
xcuserdata/
*.xcuserstate
DerivedData/
.build/

# macOS
.DS_Store

# Python (mcp-server/)
__pycache__/
*.pyc
.venv/

# Never commit real WHOOP OAuth credentials.
*.env
Secrets.plist

# Local-only WHOOP OAuth credentials for dev testing — never commit.
.secrets.local.json

# Your Apple developer team and any bundle ID override — see Config/Signing.xcconfig.
Config/Signing.local.xcconfig
