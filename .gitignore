# Xcode
xcuserdata/
*.xcuserstate
DerivedData/
.build/

# macOS
.DS_Store

# Never commit real WHOOP OAuth credentials.
*.env
Secrets.plist

# Local-only WHOOP OAuth credentials for dev testing — never commit.
.secrets.local.json
